// Apps externes ouvertes directement depuis la grille de l'accueil (clé = slug du
// projet). Sans entrée, la tuile mène à /projects/[slug].
export const APPS = {
  // Admin Spircle (« Pointe — Admin »), projet Vercel bar-finder-admin.
  spircle: { externalUrl: 'https://bar-finder-admin.vercel.app' },
};
